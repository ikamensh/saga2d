# Paid troop replacement through real input

The rendered source is `ad9026b`. The native report retains its actual starting
HEAD (`c80fb3e`); every recorded source/fixture hash was independently compared
with the later committed files and matched. The report was not rewritten to
pretend it began from a clean commit. `provenance.json` records the comparison.

- **1,108 tests passed** in 85.77 seconds at the committed source. Tribes' same
  source passed 60 AI games and 20 scene runs. Linked Shardbound stress at this
  commit passed 12 model campaigns and 12 scene runs with 2,194 input activations;
  model fuzz exercises replacement, while scene-fuzz replacement input remains
  a separate pending increment.
- Native Pyglet on macOS Retina passed **120 complete reviews**, **1,170 input
  activations** and the exact save/reloads recorded in `native.json`. Reviews
  cover ten recruits from opening and fully funded armies, both reading sizes,
  and three native windows. Keyboard and mouse each perform a paid Warden
  replacement from the same retained earned army, with Cancel, settings,
  disabled controls and manual recovery checks.
- The assault begins from that actual late paid checkpoint. New buildings and
  replacement are bought through UI; three rounds use the visible automatic
  action. Two manual aimed orders Swap Swordsman 5 from the flank and Heal it
  for 22 HP. Automatic cleanup then matches the independently saved model
  continuation exactly, with the rescued troop alive at victory on turn 11.
  Retirement is a separate loss from battle casualties. This is role access,
  not proof that replacement beats keeping or resting an army.
- Independent native review found two real directory-error overflows. The
  picker now pages complete troops; the longest review retains the full error
  alongside all quote/consequence data, replacing only the optional rest hint.
  The retained verifier covers both. The separate longer-path native reproducer
  also passed 20 inputs: picker height 736, review/applied height 738.

All seven retained PNGs were opened and inspected: the cost review, both error
roster pages, Skyrider filesystem error, failed checkpoint acknowledgement and
the Swap/Heal forecasts. Labels and controls are legible and separate. This
does not establish Windows, packaged-candidate, human-playtest or universal
layout coverage; G01–G19 remain incomplete. The earlier `0e27175` Mac artifact
does not include replacement or these reading improvements.
