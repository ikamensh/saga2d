# Campaign objective reminder and ending copy — 2026-09-06

Source **325c5c57ad1400c2bb947cb7ef816e06122e824b** keeps the current contract's
complete objective on the shard map. It uses the game's existing contract text
and measured label flow; no rule or framework interface changes. The earned
ending now says “1 troop lost” for a singular casualty.

The native check ran on isolated working source with HEAD `5ec7e56`, subsequently
committed as `a3bd451` and integrated as `325c5c5`. All **72 recorded source
hashes** and the verification script hash match the integrated source exactly.
`integration.json` records that identity check; the original native report is
unchanged. This is not an additional native execution.

The new regression first failed because the objective was absent. Six focused
tests then passed in **0.66 seconds**. The contract test follows five legally
earned contracts through province selection, campaign-plan return and F5/F9,
at 125% text across three supported window sizes.

The actual native check covers Foundries arrival, Gate arrival and the completed
directed campaign ending: **26 inputs, three exact F5/F9 joins**, unchanged game
states and confirmed window closure. It took **8.11 seconds wall / 2.06 seconds
CPU**, about 25.34% of one core, with a cooperative 25% budget and 30 FPS input.
The Foundries and ending saves come unchanged from the completed directed
journal; Gate comes from a public offer at a historical paid departure.

Both reviewers inspected the actual Foundries, Gate and ending PNGs. The agent
also inspected both selected-capital PNGs. Objectives, rival controls and
warnings remain readable at 125%; the ending retains its numeric facts with
correct singular copy. These are three saved presentation cases, not another
campaign playthrough. The full 556-command native journey is separately
attributed to `75c2b60` in the [directed campaign evidence](../directed-control-complete-718afc9/README.md).

G01–G19 remain incomplete. This increment supplies no human, platform release,
full native opening or battery-life acceptance result.
