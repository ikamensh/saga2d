# Verification receipts

These notes preserve commands and observations from the 2026-09-06 task.
Quoted process and agent outputs are copied from tool results, rather than
generated by a later replay. JSON smoke and manifest files remain the primary
automated results. `signature-check.json` records a fresh signature check.

## Focused tests

Working directory:
`/Users/ikamen/ai-workspace/experiments/by_kodo/saga2d/.claude/worktrees/shardbound-build-information`

Clean HEAD: `a6851fb65df5abcd1c923ab2c736921300a12ea6`.

```sh
uv run python -m pytest tests/eador/test_about.py tests/eador/test_launch_session.py tests/eador/test_title_reading.py tests/packaging -q
```

Exec session `92401` finished with exit 0; `focused-tests.txt` contains its
redirected output: **11 passed in 5.40s**. The final clean source includes the
Guide/About native smoke and all frame-yield changes in packaging/entry.py.

## Ordinary isolated launch

Working directory `/tmp`; command:

```sh
caffeinate -diu /tmp/shardbound-playtest-a6851fb/Shardbound.app/Contents/MacOS/Shardbound --data-dir /tmp/shardbound-blind-a6851fb
```

Exec session `80006` was confirmed live. Initial process sample:

```text
16947 83126  35.9       00:01 /tmp/shardbound-playtest-a6851fb/Shardbound.app/Contents/MacOS/Shardbound --data-dir /tmp/shardbound-blind-a6851fb
```

The final `ps -p 16947 -o pid,%cpu,etime,command` sample, immediately before
`kill -TERM 16947`, was:

```text
  PID  %CPU ELAPSED COMMAND
16947  20.4   00:27 /tmp/shardbound-playtest-a6851fb/Shardbound.app/Contents/MacOS/Shardbound --data-dir /tmp/shardbound-blind-a6851fb
```

The command tool returned exit 0, and polling session `80006` reported terminal
exit 0. A later complete process inventory contained no Shardbound/test process.
The process was deliberately closed; no gameplay or graceful player-exit claim
comes from this observation. Its captured stderr is `ordinary-launch.log`.

The earlier command `open -n /tmp/shardbound-playtest-a6851fb/Shardbound.app
--args --data-dir /tmp/shardbound-blind-a6851fb` returned exit 0, but a later
inventory showed no Shardbound process. No cause for that exit was established.

## LaunchServices smoke

```sh
caffeinate -diu open -W -n /tmp/shardbound-playtest-a6851fb/Shardbound.app --args --smoke-image /tmp/shardbound-launchservices-a6851fb/smoke.png
```

Exec session `25836` finished with exit 0. `launchservices.json` is its generated
receipt: frozen executable, working directory `/`, exact build identity,
save/settings/audio checks and unchanged campaign after Guide/About navigation.

## Independent first-run attempt

Agent `/root/blind_packaged_opening` received no conversation history or code
knowledge. It was instructed to use only native visible UI for a bounded first
run, without reading source, docs, saves or hidden APIs. It reported its first
call as `cua.getApp("/tmp/shardbound-playtest-a6851fb/Shardbound.app")` and returned:

> The Mac is locked and automatic unlock could not unlock it. Ask the user to unlock the Mac manually before continuing.

Agent report: **zero game observations, screenshots or gameplay inputs**;
process lifecycle was left to root. This is a blocked agent attempt with no
G09 credit, not a human playtest or evidence about the game's onboarding.

## Independent evidence review

Agent `/root/build_info_review` independently checked all 164 pre-commit
title-verification source hashes against `a6851fb`, report counts, source identity,
archive size/hash and fresh smoke claims. No contradicted claim was found.
Its requested retention gap is addressed by these notes and the signature
receipt. No additional game or heavy test was launched during that review.
