# Worker Notes

Context from prior work. Keep lean — replace stale entries, delete what no longer applies.

## Project
- Saga2D game framework (Python, pyglet backend)
- `uv run python -m pytest tests/ -v` for full test suite
- Visual verification mandatory for rendering changes — mock tests prove logic only
